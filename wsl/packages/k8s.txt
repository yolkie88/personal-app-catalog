# Kubernetes / K3s tools, installed and pinned through mise. bootstrap.sh reads
# this file directly, so it is the single source of truth.

kubectl@latest
helm@latest
k9s@latest
kubectx@latest
kubens@latest
stern@latest
oras@latest
