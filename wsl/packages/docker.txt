# Docker Engine packages installed inside WSL. bootstrap.sh reads this file
# directly for the final apt install step, so it is the single source of truth.
# Repository prerequisites (ca-certificates, curl, gnupg) are handled separately.

docker-ce
docker-ce-cli
containerd.io
docker-buildx-plugin
docker-compose-plugin
