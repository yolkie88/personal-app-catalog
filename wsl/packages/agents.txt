# Agentic coding CLIs. Use each vendor's official non-npm install surface.
#
# Format:
#   <binary-to-check>|apt|<apt-package>|<channel>
#   <binary-to-check>|installer|<https installer url>
#
# Auth (claude / codex login) is a manual first-run step and never stored here.
# In a restricted network run `proxy_on` before installing.

claude|apt|claude-code|stable
codex|installer|https://chatgpt.com/codex/install.sh
