# WSL-first CLI toolchain. mise manages these tools.
# bootstrap.sh reads this file directly, so it is the single source of truth.
# Entries using @latest or @lts are intentionally floating; use exact versions
# when reproducibility matters more than freshness.
# mise itself is bootstrapped separately and is not listed here.

node@lts
python@latest
uv@latest
pnpm@latest
neovim@latest
lazygit@latest
delta@latest
zoxide@latest
starship@latest
just@latest

# GitHub CLI lives in WSL (next to your repos/git), not Windows. mise provides it
# via the aqua:cli/cli backend.
gh@latest

# yq via mise is the Go (mikefarah) build; apt yq is the Python wrapper.
yq@latest
